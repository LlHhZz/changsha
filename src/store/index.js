import { createStore } from 'vuex'
import navBarControl from './modules/navBarControl'

export default createStore({
  state: {
  },
  getters: {
  },
  mutations: {
  },
  actions: {
  },
  modules: {
    navBarControl
  }
})
